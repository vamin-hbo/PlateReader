<UserControl x:Class="PlateReader.View.FileDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding FileDialog, Source={StaticResource Locator}}">
    
    <Grid>
        <WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top" Margin="0,0,0,10">
            <TextBlock Text="Please Select File: "></TextBlock>
            <Button Name="btnOpenFile" Command="{Binding OpenCommand}">Open file</Button>
        </WrapPanel>    
    </Grid>
</UserControl>
